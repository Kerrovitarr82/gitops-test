apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-demo3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx3
  template:
    metadata:
      labels:
        app: nginx3
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        resources:
          requests:
            memory: "32Mi"
          limits:
            memory: "64Mi"
