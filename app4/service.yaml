apiVersion: v1
kind: Service
metadata:
  name: nginx-demos4
  labels:
    app: nginx4
spec:
  type: ClusterIP
  selector:
    app: nginx4
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8083
