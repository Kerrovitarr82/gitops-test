apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-demo5
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx5
  template:
    metadata:
      labels:
        app: nginx5
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        resources:
          requests:
            memory: "32Mi"
          limits:
            memory: "64Mi"
