apiVersion: v1
kind: Service
metadata:
  name: nginx-demos1
  labels:
    app: nginx1
spec:
  type: ClusterIP
  selector:
    app: nginx1
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
