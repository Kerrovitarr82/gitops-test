apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-demo6
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx6
  template:
    metadata:
      labels:
        app: nginx6
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        resources:
          requests:
            memory: "32Mi"
          limits:
            memory: "64Mi"
