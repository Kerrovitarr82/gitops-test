apiVersion: v1
kind: Service
metadata:
  name: nginx-demos7
  labels:
    app: nginx7
spec:
  type: ClusterIP
  selector:
    app: nginx7
  ports:
    - protocol: TCP
      port: 8086
      targetPort: 8086
