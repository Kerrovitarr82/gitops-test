apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-demo7
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx7
  template:
    metadata:
      labels:
        app: nginx7
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        resources:
          requests:
            memory: "32Mi"
          limits:
            memory: "64Mi"
